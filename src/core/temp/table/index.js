import { reactive, computed } from 'vue'
import { convertId, convertName } from '../../../core/utils/odoo-utils'

// 问题: 如何保证A、B两个数组、循环A数组后得到一个根据B数组顺序产生的新数组

export function useCategory(props) {
  // 权限
}
